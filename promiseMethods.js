// 4️⃣ Promise Methods
// Method	Description
// Promise.all([...])	Runs all promises in parallel and waits for all to resolve/reject.
// Promise.race([...])	Returns the first promise that resolves/rejects.
// Promise.allSettled([...])	Returns the status of all promises.
// Promise.any([...])	Returns the first fulfilled promise (ignores rejections).
